parameters:
- name: filename
  type: string

trigger: none
pr: none

jobs:
- job: springfield
  variables:
  - group: common
  - template: /.azuredevops/variables-global.yml
  pool: ${{ variables.GFX942_TEST_POOL }}
  workspace:
    clean: all
  steps:
    - checkout: none
    - task: Bash@3
      displayName: Download installer
      env:
        SPRINGFIELD_URL: $(SPRINGFIELD_URL)
      inputs:
        targetType: inline
        script: wget -nv "$SPRINGFIELD_URL/rocm/${{ parameters.filename }}" -O rocm-installer.run
    - task: Bash@3
      displayName: Install ROCm
      inputs:
        targetType: inline
        script: |
          sudo DEBIAN_FRONTEND=noninteractive apt update
          sudo DEBIAN_FRONTEND=noninteractive apt install -y tzdata
          sudo DEBIAN_FRONTEND=noninteractive apt upgrade -y
          chmod +x ./rocm-installer.run
          ./rocm-installer.run target="$(Agent.BuildDirectory)" deps=install rocm postrocm
    - task: Bash@3
      displayName: List files
      inputs:
        targetType: inline
        script: ls -la1R $(Agent.BuildDirectory)
    - task: Bash@3
      displayName: Set env vars
      inputs:
        targetType: inline
        script: |
          export ROCM_PATH=$(Agent.BuildDirectory)/rocm
          export PATH=$PATH:$ROCM_PATH/bin
          export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$ROCM_PATH/lib:$ROCM_PATH/llvm/lib
          export CXX=$ROCM_PATH/bin/amdclang++
          export CC=$ROCM_PATH/bin/amdclang

          echo "##vso[task.setvariable variable=ROCM_PATH]$ROCM_PATH"
          echo "##vso[task.prependpath]$ROCM_PATH/bin"
          echo "##vso[task.setvariable variable=LD_LIBRARY_PATH]$LD_LIBRARY_PATH"
          echo "##vso[task.setvariable variable=CXX]$CXX"
          echo "##vso[task.setvariable variable=CC]$CC"
    - script: rocminfo
    - script: rocm_agent_enumerator
    - script: rocm-smi
    - script: amd-smi
    - script: amd-smi version
    - script: amd-smi list
    - script: amd-smi static
    - script: amd-smi metric

    - task: Bash@3
      displayName: Build rocDecode tests
      inputs:
        targetType: inline
        script: |
          mkdir $(Agent.BuildDirectory)/rocdecode/test
          cd $(Agent.BuildDirectory)/rocdecode/test
          cmake $ROCM_PATH/share/rocdecode/test
    - task: Bash@3
      displayName: Run rocDecode tests
      continueOnError: true
      inputs:
        targetType: inline
        script: ctest --output-on-failure --output-junit rocdecode_results.xml
        workingDirectory: $(Agent.BuildDirectory)/rocdecode/test
    - task: PublishTestResults@2
      displayName: Publish rocDecode tests
      condition: succeededOrFailed()
      inputs:
        searchFolder: $(Agent.BuildDirectory)/rocdecode/test
        testResultsFormat: JUnit
        testResultsFiles: rocdecode_results.xml
